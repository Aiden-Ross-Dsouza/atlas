# E2 — Appearance vs dynamics (2×2) config
#
# NOTHING LOADS THIS FILE (FIX_SPEC.md C9, see configs/atlas/default.yaml's
# header). Verified against scripts/run_e2.py's real argparse defaults
# 2026-08-28: routers=[umf,sdyn]/episodes=40/seeds=3 all match exactly. No
# disagreement found in this file.
defaults:
  - default

e2:
  cells:
    A: {regime_a: R0, regime_b: R0, corruption_a: none, corruption_b: none}
    B: {regime_a: R0, regime_b: R1, corruption_a: none, corruption_b: none}
    C: {regime_a: R0, regime_b: R0, corruption_a: none, corruption_b: colour_change}
    D: {regime_a: R0, regime_b: R1, corruption_a: none, corruption_b: colour_change}
  routers: [umf, sdyn]
  episodes: 40
  seeds: 3
  out_subdir: e2
