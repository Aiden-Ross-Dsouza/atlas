# P0 CLOSE-OUT AUDIT

**Date:** 2026-08-30 · **Scope:** the whole Phase-0 close-out state — FIXLOG V3-9…V3-16, the real
N=100 on-policy artifacts in `phase0_v3/p0g_onpolicy{,_r0,_frozen_check}/`, the plan-side and
ledger-side decision records, and C-1/C-2 readiness.
**Nothing was launched. No production code was changed. Review only.**

**Method.** Main-session verification (every number recomputed from raw files with
`.venv/Scripts/python.exe`; every code claim read at current source), plus a `scientific-redteam`
dispatch and a scoped `phd-skills:xray` run over three applicable dimensions — code↔claim alignment,
numerical accuracy, evaluation integrity. (X-ray's terminology and citation dimensions were dropped:
there is no `.tex` in this scope, so they would have produced nothing.) All four reports are folded
into the single table below.

**Attribution and verification status.** Per `CLAUDE.md` §1.9 every row states what was actually
done. Rows marked **[MS]** were verified by the main session directly. Rows marked **[agent, MS-confirmed]**
originated with a sub-agent and were independently re-derived here. Rows marked **[agent, not re-derived]**
are the agent's measurement only and are labelled **UNVERIFIED** where that matters.

---

## VERDICT

> ### **P0 IS NOT CLOSED.**
>
> Not because P0-G failed. It succeeded, and the R2 chart is real: the frozen-vs-trained paired
> comparison reproduces exactly from disk, the pairing is genuinely the same 8 test trajectories,
> and the chart lowers UMF by 40.5% at trajectory scale and 17.3% at the τ-relevant T=2 scale
> (V-3, V-4 below). Several of the specific failures the audit brief predicted are **not present**
> — the R1 decision *is* recorded, demo-episode leakage *is* zero, and both gate call sites *are*
> correct in current source.
>
> P0 is not closed because **Phase 0's own declared exit conditions have not been met**, and because
> one pre-registered decision rule has now been measured and never read.
>
> **The six blockers, in the order they should be dealt with:**
>
> 1. **B-4 — deal with this first, C-2 is running right now.** `run_e0_planning.py` and
>    `modal_e0_planning.py` both default to `iterations=30, num_act_stepped=6`. The paired baseline
>    was run at `iterations=10, nas=2`. `c2_planning_screen.py` pairs on `episode` index alone and
>    asserts nothing about protocol, and the baseline JSONL rows carry no planner-config fields. A
>    single omitted flag yields a clean-looking McNemar over two arms run under different protocols.
>    **Confirm the live C-2 launch passed `--iterations 10 --num-act-stepped 2` before trusting its
>    output.**
> 2. **B-1** — §11.1 states P0-A/B/D/E are *"re-derived against P0-G's `onpolicy` chunks before
>    Phase 0 closes."* Not done. This audit did the τ half: **τ = 0.290** on-policy, and **q must
>    become 4 if τ stays pinned at 0.262**. σ_r and the strike rate *cannot* be re-derived from what
>    was persisted.
> 3. **B-2** — §15-2's pre-registered contact-collapse check has its measurement and it **fails**:
>    R0 17.07 vs R2 4.50 contacts/trajectory, a **−74% collapse**, worse than the −65% replay
>    collapse that created the concern. `damping = 0.1` is the pre-registered fallback. Nobody has
>    read this number. Everything was paid for at `damping = 0.5`.
> 4. **B-3** — `EVIDENCE_LEDGER.md`, the declared single source of truth, still records P0-G as
>    *"pending"*. So does the plan's §11.1-R. A fresh session reading the two files `CLAUDE.md`
>    orders it to read first concludes P0-G never ran.
> 5. **B-5** — the close-out's headline claim is **two bare means with no CI**, a direct `CLAUDE.md`
>    §5 violation. The design *is* paired, so a CI is computable — but only means were persisted, so
>    no CI can be produced from existing artifacts.
> 6. **B-6** — `phase0_v3/p0g_onpolicy_r0/trajs_R0.pt` is **corrupt** and will not load. R0 is the
>    regime τ is defined over.
>
> **None of B-1, B-2, B-3, B-5 (partially) need GPU.** They are hours of local work on data already
> downloaded. B-4 needs one command line checked. B-6 needs a re-download.

---

## FINDINGS

Severity: **BLOCKS PHASE 1** · **TRUST** = fix before trusting the chart · **HUMAN** = needs a
recorded decision · **COSMETIC** · **VERIFIED CORRECT** = checked and found sound, recorded so it is
not re-audited.

### BLOCKS PHASE 1

| ID | Finding | Evidence | Verification |
|---|---|---|---|
| **B-1** | **The Phase-0 thresholds were never re-derived on on-policy chunks, as §11.1 requires — and doing so changes `q`.** Re-derived here from the real N=100 dumps at §6.6's own gate (P95 latent-disp over block-static chunks; R0 = 187.55): **τ = P95 UMF(c₀) over R0 informative chunks = 0.2900** (n=313 of 540), against the provisional dataset-replay proxy 0.262. R2's strike rate at that τ = **0.9364** (proxy: 0.675) — comfortably above §6.2's 0.8 requirement, so the probe still arms fast. **But** R0's strike rate *at the still-pinned proposal τ=0.262* is **0.0990**, not 0.051, and §6.2's own rule `p^q·1100 < 1` then gives **q ≥ 3.03 ⇒ q = 4**. τ and q must be adopted as a matched pair; **τ=0.262 with q=3 is internally inconsistent on on-policy data.** At τ=0.290 the false-strike rate is 0.0511 by construction and q=3 holds. | `phase0_v3/p0g_onpolicy_r0/chunks_R0.jsonl` (n=540), `phase0_v3/p0g_onpolicy/chunks_R2.jsonl` (n=540); plan §6.1, §6.2, §6.6, §11.1. | **[MS + 2 agents, independently concurring]** Recomputed with numpy, reproducing §6.1 and §6.6 literally. Ran under four gate choices to confirm the result is not an artifact of my gate choice: ungated **0.6176**, T=2 10-pct gate **0.4381**, §6.6 P95-static gate **0.2900**, legacy trajectory gate **0.2923**. The x-ray numerical agent independently obtained 0.6176 / 0.2900 and the integrity agent 0.6176 / 0.2900 / 0.2631-at-the-legacy-242.7-gate. **Sample-constancy disclosure (§5): this is a different sample from P0-D's** — 540 on-policy chunks over 108 trajectories vs 137 dataset-replay informative chunks. It is a proxy→final replacement by design, not a like-for-like correction. |
| **B-2** | **§15-2's pre-registered contact check has been measured and never applied.** On-policy training trajectories: **R0 mean 17.07 contacts/traj, R2 mean 4.50 — a 74% collapse.** The concern §15-2 exists to settle was a replay collapse of 38.5→13.3 (−65%); the mitigating hope in `P0G_REVIEW.md` §E was a live-planner 12.5→6.9 (−45%). The real on-policy number at N=100 is **worse than both**. `damping = 0.1` is the pre-registered fallback. The chart, the frozen baseline and the gate calibration were all committed at `damping = 0.5` without this being read. The only on-policy contact figure recorded anywhere is an **n=9/regime smoke** (FIXLOG V3-11: R0 16.3 vs R2 6.7). | `phase0_v3/p0g_onpolicy/e0_seed_manifest.json` vs `…_r0/e0_seed_manifest.json`, `n_contacts`, 100 train trajectories each; plan §4 and §15-2. | **[MS]** Loaded both manifests and averaged `n_contacts` over the 100 train trajectories per regime. Zero-contact counts R0 6/100, R2 6/100. **P12 is genuinely fixed** — the number is persisted, which is the only reason this was checkable. It still gates nothing. Corroborated independently by the red-team agent (Q10). |
| **B-3** | **Neither file `CLAUDE.md` orders a fresh session to read first contains a single P0-G number.** `EVIDENCE_LEDGER.md:116` still reads `P0-G onpolicy collection │ blocked on P0-C │ pending │ ~$15`. `IMPLEMENTATION_PLAN_V3.md` §11.1-R still reads `not started — needs a funded Modal account`. Real money was spent, both regimes collected, a chart trained, a frozen baseline paid for. The only record is `FIXLOG.md` — a change log, not the numbers register — plus raw JSON. `CLAUDE.md` designates the ledger as winning over every other document on any number. | `EVIDENCE_LEDGER.md:116`; `IMPLEMENTATION_PLAN_V3.md` §11.1-R. | **[MS + 2 agents]** Grepped the ledger for `P0-G`/`onpolicy`/`p0g` and read all 10 hits — every one is forward-looking ("to be re-derived", "once P0-G runs"); none reports a result. Read §11.1-R in full. |
| **B-4** | **URGENT — C-2 cannot detect a protocol mismatch, and the launcher defaults are the wrong protocol.** `c2_planning_screen.py` pairs two arms on `episode` index alone (`:34-70`) and never compares planner config; the baseline JSONL rows carry `regime_config` but **no** `num_samples`/`iterations`/`horizon`/`num_act_stepped`/`max_steps`. Meanwhile `run_e0_planning.py:401,403` and `modal_e0_planning.py:81,83` both default `iterations=30, num_act_stepped=6`, while the paired baseline `p0c_it10_baseline_R2.jsonl` was run at `iterations=10, nas=2`. One omitted flag produces a chart arm at the wrong protocol that pairs "successfully" and is reported as a valid McNemar. **This is the fourth instance of the like-for-like defect class the brief asked for, moved into the planner protocol — and it is worse than its three predecessors because the mismatch would be invisible in the output.** | `scripts/c2_planning_screen.py:34-70`; `scripts/run_e0_planning.py:401,403`; `modal/modal_e0_planning.py:81,83`; `phase0_v3/p0c/p0c_it10_summary.json`; `phase0_v3/p0c/p0c_it10_baseline_R2.jsonl`. | **[2 agents, MS-confirmed]** MS read the screen script end to end and confirmed `episode` is the only key compared, and read the baseline summary's config block (`num_samples 300, iterations 10, horizon 6, num_act_stepped 2, damping 0.5`, episodes 0–19, 10/20 successes, all fields the screen needs present). The integrity agent additionally cross-derived nas=2 from the JSONL itself (`steps: 30, replans: 3` ⇒ 10 raw steps/replan). **Required C-2 launch flags: `--iterations 10 --num-act-stepped 2`**; `--max-steps 30`, `--min-block-pos-diff 40.0`, `--max-agent-block-dist 160.0` already match by default. |
| **B-5** | **The close-out's headline claim is two bare means with no CI, n=8, one seed, one launch — a direct `CLAUDE.md` §5 violation ("Every table reports Δ with a CI — never two bare means").** FIXLOG V3-16 concludes "the chart genuinely helps … ~40% / ~17% UMF reduction" from 0.5568→0.3313 and 0.6753→0.5583. The design **is** paired (byte-identical test seeds, V-3), so a CI is computable — but `_umf_detail_fields` records only means, counts and gate values, never the per-item scores, so **no CI can be produced from existing artifacts**. Separately, the single R2 fine-tune has never been rerun and diffed, and §4.1's P16 determinism-asymmetry check is still `PREPARED, NOT RUN` (the `--steps 0` control has no backward pass, so it does not exercise it). Every number in `results.json` is a single-launch value carrying neither a repeat nor an UNVERIFIED label. | `phase0_v3/p0g_onpolicy/results.json` and `…_frozen_check/results.json` — only `eval_umf`, `eval_umf_chunkT2`, `eval_n_umf: 8`, `eval_n_umf_chunkT2: 40`; no per-item arrays. `scripts/run_e0.py:759-773`. `CLAUDE.md` §5, §1.9. | **[agent, MS-confirmed]** MS read both `results.json` in full and confirmed no per-item field exists; read `_umf_detail_fields`. Fix is cheap (~$0.2): dump per-trajectory and per-window UMF for both arms and run `paired_bootstrap` unmodified. |
| **B-6** | **`phase0_v3/p0g_onpolicy_r0/trajs_R0.pt` is corrupt and will not load.** 320,758,121 bytes (vs R2's 320,758,377, which loads fine), so it is not truncated — `zipfile.testzip()` fails at member `data/120`/`data/121`. Guard metadata is still recoverable from `data.pkl` (`num_train_trajs=100`, `train_traj_len=30`, `collect_cem='300x10 nas=2'`) but **no tensors load**. FIXLOG V3-13 records this merge as a clean success. Consequences: R0's trajectory-scale `motion_gate` (180.7) and `block_static_R0.json`'s splits are not independently recomputable; R0 cannot be fine-tuned or re-evaluated; and B-2's `damping=0.1` fallback, if invoked, would need R0 re-collected. **R0 is the regime τ is defined over (§6.1).** | `phase0_v3/p0g_onpolicy_r0/trajs_R0.pt`. | **[MS + 3 agents, all concurring]** MS ran `os.path.getsize` and `torch.load(..., map_location="cpu", weights_only=False)` on both files: R2 → `LOADS OK, keys ['guard','train','val','test']`; R0 → `RuntimeError: PytorchStreamReader failed reading file data/121: invalid header or archive is corrupted`. Re-download from the `atlas-data` Modal volume before using R0 for anything. |

### TRUST — fix before trusting the chart

| ID | Finding | Evidence | Verification |
|---|---|---|---|
| **T-1** | **V3-15's Bug-4 gate-scale fix reached the code and R2's artifact, but never R0's — and FIXLOG V3-16's summary table propagates wrong numbers for *both* regimes.** `gate_calibration_R0.json` records `gate_10pct_RETIRED = 180.6878`, which V3-16's own table identifies as R0's **T=6 trajectory-scale** `motion_gate`, compared against `latent_disp` values that are always T=2 windows. Reported false-pass **6.99%**; at the correct T=2 gate it is **64–66%**, a ~9× understatement — in the exact artifact designated as the §15-5 motion-gate sign-off evidence. Separately, V3-16's table reports R2's 10-pct false-pass as **0.0%**, a value V3-15 explicitly **retracted two sections earlier in the same file**; the corrected artifact on disk says **0.25**. | `phase0_v3/p0g_onpolicy_r0/gate_calibration_R0.json:6`; `phase0_v3/p0g_onpolicy/gate_calibration_R2.json:6-7`; `FIXLOG.md` V3-16 "Real N=100 collection numbers" table. | **[MS + 2 agents]** MS recomputed the T=2 gate from `chunks_R0.jsonl` three ways — 10th pct over the 500 train rows = **106.45 → false-pass 0.6573**; over all 540 = **107.35 → 0.6434**; the x-ray agent's train-row `torch.quantile` = **104.37 → 0.6643**. **The exact corrected value is UNVERIFIED** because FIXLOG's 104.4 derives from the raw encoder outputs in the corrupt `trajs_R0.pt` (B-6); all three reproducible variants land in 64–66%, so the finding is robust to the gate choice. `gate_p95_over_block_static` (187.55 / 197.12) and its false-pass rates **do** reproduce exactly in both regimes — only `gate_10pct_RETIRED` is affected. |
| **T-2** | **Cross-artifact drift on `train_loss`: `results.md` — the paper-facing T5 table — still carries the stale value `results.json` was patched away from.** `results.json` = `0.22234542042016983`; `results.md` = `0.184429`. Drift **0.0379 absolute, 20.6% relative**. Independently traced in the raw array: the patched value is **exactly `loss_ln_act_R2.json[249]`** (step 250, the best-val checkpoint — V3-15's patch is *correct*), and `0.184429` is **exactly `[374]`**, the final step. V3-15's manual patch reached two of the three artifacts. The T5 row therefore mixes two checkpoints: train loss from step 375, eval loss/UMF from step 250. | `phase0_v3/p0g_onpolicy/results.md` vs `results.json` vs `val_loss_ln_act_R2.json`; `loss_ln_act_R2.json` indices 249 and 374 (n=375). | **[MS + 3 agents]** MS loaded the 375-element array and searched it for both values to full precision: index 249 matches to 1e-9 and is the **only** exact match; index 374 matches `results.md`. The numerical agent independently confirmed and added that mtimes corroborate staleness (`results.md` 03:09:19 predates `results.json` 03:37:12). |
| **T-3** | **The R0 and R2 P0-G collections are UNPAIRED at task level, so any R0-vs-R2 comparison confounds the regime shift with goal-difficulty variance.** `seed_base = {"R0": 2000, "R2": 1000}` and that same seed seeds the `RandomState` drawing `(episode_idx, offset)` — the two arms get **different tasks**, not the same tasks under different physics. This is what gate **G5** exists to forbid ("same seeds, two arms ⇒ identical initial states/goals"). Magnitude: only **2/100** train episodes in common; R2−R0 mean UMF = **+0.370** while the between-trajectory sd of mean UMF is **0.071 (R0) / 0.106 (R2)**, so task variance is a large fraction of the effect being attributed to physics. This affects the R0-vs-R2 contact comparison in **B-2** and every R0/R2 gate or τ contrast. | `scripts/run_e0.py:180,220-221,245-250`; both `e0_seed_manifest.json`. | **[agent, MS-confirmed at source]** MS read the seed-base and `RandomState` construction and confirmed the mechanism. **The 2/100, +0.370 and sd figures are the integrity agent's measurement and were not re-derived here — treat the magnitudes as UNVERIFIED, the mechanism as confirmed.** Note B-2's direction (−74%) is far larger than the sd, so B-2 survives this caveat; a fully paired re-measurement would still be the right way to report it. |
| **T-4** | **The merged `chunks_*.jsonl` cannot support per-episode statistics at all, and silently mis-groups if anyone tries.** Rows carry `regime/traj/window/umf_c0/latent_disp/block_disp_px` — no seed, no `episode_idx`, no split tag — and the `traj` field collides 4-way across shards: 540 rows but only **33 distinct `traj` ids**, ids 0–24 appearing **20 times each** (4 shards × 5 windows), with 3 descending resets in file order. So no per-trajectory, per-seed or clustered statistic is derivable — exactly what a σ_r derivation or a proper CI needs. It also silently pools the 8 val trajectories into the τ population (108 × 5 = 540; test excluded), so every gate in `gate_calibration_*.json` is calibrated on train+val. `merge_p0g_shards.py:118-119` calls the collision "harmless — nothing downstream groups by that field", which licenses the trap rather than closing it. | `phase0_v3/p0g_onpolicy_r0/chunks_R0.jsonl`; `scripts/run_e0.py:498-501`; `scripts/merge_p0g_shards.py:118-119`. | **[MS + 2 agents]** MS counted `traj` multiplicities and located the descending resets with collections/numpy, and cross-checked 108 = 100 train + 8 val against the manifest's split sizes. |
| **T-5** | **V3-13's manifest-reconstruction fix silently dropped `_provenance`, undoing P20 for both merged regimes.** `p0g_onpolicy/e0_seed_manifest.json` and `…_r0/…` have top-level keys `['R2']` / `['R0']` only. The un-merged control `p0g_onpolicy_frozen_check/…` **does** carry `_provenance` with `git_commit: b09fc1c…`, torch 2.7.0+cu126, CUDA 12.6, device NVIDIA L4 and the determinism settings. So the *control* arm is version-attributable and **the chart the whole phase produced is not**. Same class as P20, reintroduced by a fix for something else. | Manifest top-level keys, all three directories; `FIXLOG.md` V3-13. | **[MS + 1 agent]** Loaded all three manifests, compared top-level keys, printed the frozen-check `_provenance` block verbatim. |
| **T-6** | **Neither motion gate fired on either evaluation set, and no artifact says so.** In both runs `eval_umf == eval_umf_ungated` and `eval_umf_chunkT2 == eval_umf_chunkT2_ungated` to every digit, with `eval_n_umf = 8 = eval_n_trajs` and `eval_n_umf_chunkT2 = 40 = eval_n_windows`. The "ungated" control V3-9 §4.4 added specifically so gating effects would be visible carries **zero information** in the only two runs that exist, and the acceptance number is untested against gating. Plausible benign cause: `block_static_R2.json` shows the test split is 0/8 block-static. Worth stating explicitly rather than letting "gated" imply a gate acted — and §7.1's G7 requires a firing rate that is not ~0%. | `phase0_v3/p0g_onpolicy/results.json` and `…_frozen_check/results.json`. | **[2 agents, MS-confirmed]** MS read both files and compared the four float pairs digit by digit. |
| **T-7** | **C-1's pooled-ρ trap is still live at the emitting end.** `analyze_cost_ranking.py` is clean — it propagates only `mean_of_per_seed_rhos` and `ci95_of_mean_seed_rho` (`:61-62`). But `diagnose_cem_costs.py:412` still computes a pooled `spearmanr`, stores it as `pooled["pooled_spearman_rho"]` (`:426`) and **prints it first** on the console line (`:422-424`). Measured gap on a real output: `atlas_out/cost_ranking_R0/cost_ranking_R0_seeds0-9.json` gives **pooled ρ 0.741 (n=3000 pseudo-replicates)** vs **per-seed mean 0.501, sd 0.362, CI [0.277, 0.726]** — inflated by 0.24, exactly as `EVIDENCE_LEDGER` N11 warns. **Whoever reads C-1's console output will see the wrong number first.** | `scripts/diagnose_cem_costs.py:412,422-427`; `scripts/analyze_cost_ranking.py:13,61-62`; `atlas_out/cost_ranking_R0/cost_ranking_R0_seeds0-9.json`. | **[agent, MS partially confirmed]** MS grepped `analyze_cost_ranking.py` and confirmed it emits per-seed statistics only. **The pooled-vs-per-seed 0.741/0.501 figures are the integrity agent's measurement and were not re-derived here — UNVERIFIED magnitude, mechanism confirmed by the emitting code path.** |
| **T-8** | **`_reported_train_loss`'s fallback fires silently on a stale file, and its docstring denies that branch exists.** Docstring (`run_e0.py:935-936`): "fall back to the final loss **only if that file doesn't exist**". The code (`:938-942`) also falls back when the file exists but `best_train_loss` is missing or `null`, writing the wrong-snapshot final-step loss into `results.json["train_loss"]` with no warning and no provenance flag — silently reinstating Bug 5. FIXLOG calls this "expected, not a bug"; nothing in the artifact records which branch produced the number. | `scripts/run_e0.py:929-942`. | **[agent, executed]** The alignment agent **executed all four branches** against synthetic `val_loss_*.json` states in a temp dir: stale file → 0.184, field present → 0.2223, `null` field → 0.184, no file → 0.184. **[MS]** confirmed the docstring/code divergence at source. |
| **T-9** | **`tests/test_p0g_guard.py` cannot catch the regression it names.** It builds *both* command lines itself from `_p0g_spec` (`:28-46`) and imports `_p0g_spec as mp` — never `modal_phase0`. So it verifies `_p0g_spec`'s self-consistency, not that `p0g_collect`/`p0g_finetune` still *use* it. Deleting `*_p0g_flags(...)` from `p0g_finetune`'s `cmd` (`modal_phase0.py:202`) — the literal §7-B1 defect — leaves the test green. Its own docstring (`:3-5`) asserts it guards those two functions. | `tests/test_p0g_guard.py:3-5,20,28-46` vs `modal/modal_phase0.py:138-143,198-204`. | **[agent]** The alignment agent read both and traced the imports, and ran `pytest tests/ -q` → **22 passed in 4.04s** (matching V3-9's "22/22"). **UNVERIFIED:** it did not actually mutate `modal_phase0.py:202` and re-run to demonstrate the test stays green. |
| **T-10** | **τ is calibrated on stride-1 sliding windows, ~40% of which straddle a CEM replan boundary — a window type the deployed loop never scores.** `dump_regime_chunks` steps `range(0, T-nas+1)` (stride 1); deployment scores exactly one aligned, non-overlapping T=nas window per replan. Measured effect: R0 aligned P95 **0.6449** (n=324) vs straddling **0.6009** (n=216) vs pooled **0.6176** — τ biased by **−0.027**. Real but small next to B-1. | `scripts/run_e0.py:489` vs `scripts/run_e0_planning.py:326-360`. | **[agent, not re-derived]** Integrity agent's measurement; **magnitudes UNVERIFIED**, the stride-vs-alignment mechanism confirmed by MS reading `:489`. |
| **T-11** | **Two merge-script defects that weaken the artifact's own provenance.** (a) `merge_p0g_shards.py:160` recomputes the trajectory-scale `motion_gate` on the merged set and then **discards it** — never written to any file, stdout only — while the module docstring justifies the merge by "a per-shard motion_gate … must not be reported as if it were". V3-16's 287.9 / 180.7 therefore have no merge-side artifact provenance. (b) `:147` **hardcodes `"source": "closed_loop"`** into the reconstructed manifest while the true value sits unused in `merged_guard["source"]`, so a `dataset`/`hybrid` merge would write a manifest misdescribing its own provenance. | `scripts/merge_p0g_shards.py:147,160`, docstring `:5-8`. | **[agent, MS spot-confirmed]** Agent read the whole file and grepped `motion_gate` uses after `:160` (none). MS confirmed `:160` and `:164` at source while verifying V-5. |
| **T-13** | **Population sensitivity of the frozen-`c₀` T=2 UMF is roughly twice the chart effect being claimed — so "the chart helps at the τ-relevant granularity" cannot be read off one population without naming it.** Three frozen-`c₀` R2 T=2 numbers exist on disk, over three populations: **0.4491** (mean of the 18 per-episode `umf_mean` values in the real CEM-planned baseline, n=20 episodes, 2 gated to `None`), **0.5681** (smoke chunks, n=35), **0.6753** (P0-G test windows, n=40). The claimed effect is 0.6753→0.5583 = **0.117**; the frozen-only spread across populations is **0.226**. Notably the trained chart (0.558) is *worse* than the frozen model measured on real planning replans (0.449). **This is not a contradiction** — these are genuinely different populations, and V-3's paired within-population comparison remains valid. It is a caveat that must travel with the headline number, and it is the same population-conflation shape as the three named instances. | `phase0_v3/p0c/p0c_it10_baseline_R2.jsonl` (18/20 episodes carry `umf_mean`); `phase0_v3/p0g_smoke_v3/chunks_R2.jsonl`; `phase0_v3/p0g_onpolicy/results.json`. | **[agent, MS-recomputed]** The red-team agent derived 0.4491 by hand-summing grepped values and explicitly asked for a machine recompute. MS did so: **n=18 of 20, sum 8.083795, mean 0.449100, median 0.462449** — matching the agent's hand arithmetic to 4 dp *and* matching `p0c_it10_summary.json`'s own `umf_mean_of_means: 0.44909973683864746` exactly. The smoke median 0.5681 was independently confirmed by the numerical agent (n=35). |
| **T-14** | **`phase0_v3/p0g_onpolicy_frozen_check/` contains a file named `chart_ln_act_R2.pt` that is the identity / no-op chart — a live footgun for C-1 and C-2, which select charts by directory string.** `modal_e0_planning.py:296-299` resolves a chart as `{charts_root}/{charts_subdir}/chart_{kind}_{regime}.pt`. Pointing either acceptance check at the frozen-check directory would silently plan with frozen `c₀` while logging `kind=ln_act`, producing output **indistinguishable in the JSONL from a real chart arm** — i.e. C-2 would compare frozen against frozen and report a clean null. FIXLOG V3-16 notes the file was "archived per §1.7" (correct, per the never-delete rule) without noting the ambiguity its name creates. Rename it, or record the exact `--charts-subdir` C-1/C-2 must use. | `phase0_v3/p0g_onpolicy_frozen_check/chart_ln_act_R2.pt`; `modal/modal_e0_planning.py:296-299`; `FIXLOG.md` V3-16. | **[agent, MS-confirmed]** MS confirmed the file exists at that path (`os.path.exists` → `True`) and that the real chart sits at the same basename under `p0g_onpolicy/`. The agent read the chart-path resolution and the pickle headers of both. |
| **T-12** | **`derive_and_report_motion_gate` crashes on `chunk_motion_gate=None`.** `compute_motion_gates` legitimately returns `None` for the chunk gate when `T_model < nas` (`run_e0.py:972-974`); `:1235` passes it through unconditionally and `:601` does `float(current_gate_10pct)` → `TypeError`. The annotation at `:571` says `float`, so producer and consumer disagree. Same at `merge_p0g_shards.py:164`. | `scripts/run_e0.py:571,601,972-974,1235`; `scripts/merge_p0g_shards.py:164`. | **[agent, static]** Type contract read end-to-end. **UNVERIFIED by execution** — no `T_model < nas` case was constructed to trigger it. Latent, not live, at the current config. |

### HUMAN — needs a recorded decision

| ID | Finding | Evidence | Verification |
|---|---|---|---|
| **H-1** | **C-2's RED FLAG 2 fires on the expected null, contradicting the script's own power statement — decide its semantics in writing *before* the number is read.** `rf2 = (c_ka >= b_ka) and (c_final >= b_final)` → verdict `"BLOCK -- red flag fired"`. The same file's `power_statement` says the expected effect is ≈0 and "a null means 'not broken'". Under a true null both statistics are worse-or-equal with probability ~¼ or more, so the pre-registered gate will BLOCK the chart in a substantial fraction of *expected* outcomes — inviting a post-hoc override, which is precisely the `CLAUDE.md` §1.8 failure this whole audit exists to prevent. The script's own comment at `:99-101` already acknowledges RF2 trivially fires on identical inputs. | `scripts/c2_planning_screen.py:99-102,120-126`. | **[agent, MS-confirmed]** MS read the boolean and both blocks at source. Note this is a **design question, not a bug** — the plan's literal wording is faithfully implemented. It needs a human ruling on what RF2 means under a null, recorded before C-2's output is read. |
| **H-2** | **The concurrent-shard `--out-subdir` collision is still only documented, not enforced — a live landmine for the next sharded launch.** `modal_phase0.py:289` still derives `shard_subdirs = [f"{out_subdir}_shard{i}" …]` with no regime namespacing and no rejection of a directory already holding another regime's un-namespaced files. The only trace in code is an explanatory *comment* at `merge_p0g_shards.py:141`. `run_e0.py:1197-1207`'s runtime assertion covers seeds, not output paths. **Fix before the next sharded launch, not after** — this cost a failed merge and a manual recovery last time. | `modal/modal_phase0.py:289`; `scripts/merge_p0g_shards.py:141`; `FIXLOG.md` V3-16 closing block. | **[MS + 2 agents]** MS grepped both files for any collision assertion — the only hit is the comment. |
| **H-3** | **`CLAUDE.md`'s own pre-P0-G checklist item 1 — the retroactive classification of every standing Phase-0 conclusion as *verified-before-reported* / *verified-only-after-challenge* / *never-challenged* — has no artifact anywhere in the repo, and P0-G launched regardless.** `CLAUDE.md` states the four items must be done "in order" *before* P0-G is authorised. Items 2, 3 and 4 were addressed in `P0G_REVIEW.md`; item 1 was flagged there as outstanding and still is. Given that this audit found τ's freeze-target derivation does not transfer to on-policy data (B-1), item 1 is not ceremonial — it is the check that would have caught it before the money. | Repo-wide grep for `retroactive classification` / `verified-before-reported` / `never-challenged` → single hit, `research_audit/P0G_REVIEW.md`. `CLAUDE.md` PHASE 0 / VERIFICATION NOTE item 1. | **[MS]** Ran the grep across `research_audit/` and repo-root markdown. |
| **H-4** | **§6.6's invariant — "the calibration call must derive its trajectory length from `frameskip × num_act_stepped` … Assert it in G6" — is not implemented.** The gate is calibrated at `args.collect_num_act_stepped` (`run_e0.py:1224-1225`) but applied at deployment under `args.num_act_stepped` (`run_e0_planning.py:403`, default **6**) — two independent flags in two scripts with different defaults, and nothing asserts they agree. The *specific* bug (§7-B2) is fixed; the *class* is not fenced, which is what §6.6 explicitly asked for. A dormant sibling: `--train-traj-len` defaults 25 and `--eval-traj-len` 50, so the trajectory-scale gate is by default calibrated at T=5 and applied at T=10 — **not triggered in the P0-G artifact** (both are 30, confirmed from `trajs_R2.pt`'s guard). | `scripts/run_e0.py:809,829,1224-1225`; `scripts/run_e0_planning.py:403`; `scripts/smoke_gates.py` `gate_g6` region. | **[agent, MS spot-confirmed]** Agent grepped `gate_g6` for `nas`/`num_act_stepped`/`frameskip` assertions — none. MS confirmed the two default values at source and that the real run used 30/30. |
| **H-5** | **Only 2 of the 4 quantities §5 deviation-note-1 promised can be re-derived from what was persisted.** `chunks_{regime}.jsonl` carries `umf_c0`, `latent_disp`, `block_disp_px` only — enough for **τ (§6.1)** and the **motion gate (§6.6)**, and **not** for **σ_r** (§6.3's per-router IQR over `umf`/`e1`/`sdyn`) or the **library-relative strike rate** (§6.2 / P0-D). Those need a forward pass over `trajs_*.pt` — cheap, but unscheduled, and blocked for R0 by **B-6**. Until it happens, σ_r and the strike rate remain the dataset-replay proxies the ledger itself flags "provisional", and §1.7's proposed freeze target cannot be signed off as on-policy. | `phase0_v3/p0g_onpolicy_r0/chunks_R0.jsonl` field list; `IMPLEMENTATION_PLAN_V3.md` §11.1 "Provisional-vs-final"; `EVIDENCE_LEDGER.md:337`. | **[MS + 2 agents]** MS read the chunk schema directly from the raw file and both plan/ledger clauses. |
| **H-6** | **The R1 task-overlap figure has been reported as three different numbers and the earlier two were never retracted.** `P0G_REVIEW.md` P13b says 39/100 (sub-agent, explicitly UNVERIFIED); `P0G_FIX_PLAN.md` §4.2 repeats 39/100 with a "re-verify before presenting" instruction; FIXLOG V3-7 and plan §8.1 both now say **50/100** ("§3.2's fix raised it 15→50"). Three numbers, one reconciliation, no retraction in the two documents still carrying 39. Since the decision it supports is already made (V-1) and does not turn on the exact figure, this is traceability, not substance — but it is the kind of residue that becomes a reviewer question. | `P0G_REVIEW.md` P13b; `P0G_FIX_PLAN.md` §4.2; `FIXLOG.md` V3-7; `IMPLEMENTATION_PLAN_V3.md` §8.1. | **[MS]** Read all four passages. **The 50/100 figure is UNVERIFIED by this audit** — not re-derived. |

### VERIFIED CORRECT — checked, sound, recorded so it is not re-audited

| ID | Finding | Verification |
|---|---|---|
| **V-1** | **The R1 scope decision IS recorded, dated, and cites all three required bases — the brief's premise that the block is still open is wrong.** §8.1's Arms bullet reads `on **R2** only` with a bracketed `[R1 DROPPED — explicit human sign-off 2026-08-29]` block citing (i) G4's prediction-level-only result with the ±13 px null band and its flat 40→200-step behaviour, (ii) the 50/100 task overlap from `seed_base["R1"]=0`, and (iii) the §8.3 consequence, noting the foreclosures are "foreclosed, not deferred". The decision rule was edited from "at least one regime" to "in R2" with the prior wording preserved inline; §8.3's cell-B replicate is struck through with the same date and sign-off. **No action needed.** | **[MS]** Read `IMPLEMENTATION_PLAN_V3.md` §8.1 Arms + Decision rule and §8.3 Cells at source. |
| **V-2** | **Zero demo-episode leakage between the P0-G R2 training set and C-2's planning eval — independently re-run, not asserted. This also resolves the red-team agent's Q1 (which flagged that the R2 overlap check had never been redone after the §3.2 goal-separation fix).** 100 distinct train episodes, 8 val, 8 test, all three mutually disjoint at episode level, and **zero overlap with the 20 demo episodes planning seeds 0–19 draw**. The script's internal assertion (replayed RNG must reproduce the real `init_state`) passed, so the replay did not silently drift. `scripts/audit_e0_train_planning_overlap.py` now runs on an on-policy manifest — **P18's `episode_idx` fix delivered exactly what it promised.** | **[MS, executed]** `.venv/Scripts/python.exe scripts/audit_e0_train_planning_overlap.py --manifest phase0_v3/p0g_onpolicy/e0_seed_manifest.json --num-planning-seeds 20` → `Overlap (TRAIN vs planning): []` … `No train/planning-eval overlap -- no leakage detected.` Also confirmed seed- and episode-level disjointness straight from the manifest. |
| **V-3** | **The frozen-vs-trained paired UMF comparison is clean and reproduces from disk.** Frozen `c₀` 0.5568 / T=2 0.6753 / loss 0.8337; trained `ln_act` 0.3313 / 0.5583 / 0.4721 — all six match FIXLOG V3-16 exactly. The pairing is real: **train/eval/test seed lists are byte-identical between the two arms**, all three splits mutually disjoint, and both runs used identical `motion_gate_value` 287.867 and `motion_gate_chunk_value` 155.517. Reductions recompute to **40.50%** and **17.33%**. **The brief asked for a fresh `--steps 0` re-run; that was not done — this is a review-only pass and a re-run is Modal spend.** What was done is stronger than trusting a transcript: the numbers were read from the independently-downloaded artifact of a real Modal run (`ap-k4cuprPnxCkfWQDZ785EkY`). Labelled: **verified against artifact, not re-executed.** | **[MS + 2 agents]** MS loaded both `results.json`, compared all six fields, and asserted list equality on all three seed splits (all `True`) with empty pairwise intersections. |
| **V-4** | **The control really is frozen `c₀`, by control flow rather than by assertion.** `harness.py:162` — at `n_steps=0` the training loop `range(0)` never executes, `best_params` stays `None`, and the `else` branch `chart.update_from_predictor_(predictor)` (`:245`) snapshots the untouched predictor. And the trained arm really is the step-250 best-val checkpoint: `chart._params = best_params` is restored before `chart.save()` (`:240-246`). | **[agent, MS spot-confirmed]** Alignment and red-team agents both read the control flow; MS confirmed the `best_train_loss` capture point independently (see V-6). |
| **V-5** | **Both `derive_and_report_motion_gate` call sites currently pass `chunk_motion_gate`, and the merge recomputes gates on the merged set rather than copying a shard's.** `run_e0.py:1235-1236` and `merge_p0g_shards.py:164-165` both pass the T=2 value; `merge_p0g_shards.py:160` calls `compute_motion_gates(all_train, nas, …)` on the merged population, `:161` reports block-static on merged splits, and `:78-83` asserts seed-uniqueness across shards. **The merge does not average averages.** This makes T-1 purely an artifact-staleness problem, not a live code bug. | **[MS + 1 agent]** MS grepped every call site repo-wide and read each with context; read the merge's gate-recomputation block. |
| **V-6** | **`best_train_loss` is captured at exactly the same point as `best_params` — a step-count approximation was the suspected defect and it is not present.** `atlas/harness.py:226-229`: `best_train_loss = avg_loss` and `best_params = copy.deepcopy(...)` are **consecutive statements inside the same `if val_loss < best_val_loss:` block**, on the same val-check iteration, with `avg_loss` being that step's own train loss (`:190-191`); written at `:255`. Independently corroborated by the data: the patched value is `loss_ln_act_R2.json[249]` = step 250 = the `best_val_loss` step. | **[agent, MS-corroborated numerically]** Alignment agent read the loop; MS confirmed the index-249/step-250 correspondence from the raw arrays (T-2). |
| **V-7** | **P3 is genuinely closed and the A4 val→test bias is real, not zero-by-construction.** `eval_umf_source: "test"`; the test split is separately collected at `seed_offset = 20_000`. `best_val_loss = 0.38382` at step 250, `stopped_early_at_step = 375` (= 250 + 5×25, exactly `patience=5 × eval_every=25`), val monotone rising after the best. Test `eval_loss = 0.47212` > best val — a **+0.088** bias, precisely what A4 exists to expose, and identically 0 under the old `--num-test-trajs 0`. `val_umf` (0.3228) is reported separately and must not be cited as the acceptance number. | **[agent, MS-confirmed]** MS read `results.json` and `val_loss_ln_act_R2.json` in full. |
| **V-8** | **`trajs_R2.pt` is intact — a truncation alarm raised during the audit is REFUTED.** One agent reported it as possibly 881.7 KB (a Read-tool size limit message, not the file size). Actual: **320,758,377 bytes, `torch.load` succeeds, keys `['guard','train','val','test']`.** Recorded so the false alarm is not propagated. | **[MS, executed]** `os.path.getsize` + `torch.load(..., map_location="cpu", weights_only=False)`. |
| **V-9** | **C-2's screen script is otherwise sound and its power caveat is structurally inseparable from the result.** It reports the discordant-pair split (n_10/n_01/n_11/n_00), uses `mcnemar_paired` + `paired_bootstrap` from `atlas/stats.py` **unmodified**, implements both red flags to the plan's literal wording, and — the part §4.5 and §6-5 care most about — writes `power_statement` into the JSON **and** prints it, phrasing a clean verdict as `"not catastrophic (NOT 'works' -- see power statement)"`. The paired baseline checks out: n=20, episodes 0–19, **10/20**, every required field present, config matching §4.5 exactly. The remaining risks are B-4 (protocol assertion) and H-1 (RF2 semantics), not the screen's statistics. | **[MS]** Read the script end to end; loaded the baseline JSONL and confirmed row count, episode set, success count and field presence; read the summary's config block. |
| **V-10** | **The chunk arithmetic is consistent end to end.** `540 = (100 train + 8 val) × 5` T=2 windows over a T=6 trajectory, matching `dump_regime_chunks`'s train+val scope; `eval_n_windows = 40 = 8 test × 5`. Both regimes report exactly 540. R2's gate values reproduce bit-exactly from the raw data (`gate_10pct_RETIRED = 155.51747131347656`; independent recompute from `trajs_R2.pt` encoder outputs = 155.5172882080078, a 1.8e-4 CPU-vs-GPU norm difference). Smoke reference confirmed: n=**35**, median **0.56814** — matching V3-15's 0.568 exactly. | **[MS + agents]** MS verified the split arithmetic against the manifest; the numerical agent verified the bit-exact gate reproduction and the smoke median. |
| **V-11** | **No FIXLOG *code* claim in V3-9…V3-16 disagrees with current source.** Every named fix physically exists at the cited responsibility: `_p0g_spec.py`'s single source of truth (V3-9 §7-B1); `chunk_motion_gate` confined to windowed calls (§7-B2); `traj_idx_offset` and the sharding flags (V3-12); manifest reconstruction from trajectory objects with no shard-manifest read (V3-13); `map_location="cpu"` + `_to_device` over exactly `(encoder_output, actions, proprio)` (V3-14); both gate call sites (V3-15 Bug 4); `best_train_loss` capture (Bug 5); the `--steps 0` control (V3-16). Only line numbers drift (V3-15 cites `harness.py:235-241`, actual `:240-246`). **The disagreements this audit found are between FIXLOG's reported *numbers* and the artifacts on disk (T-1, T-2), and in defects the fixes left open — not in the fixes themselves.** | **[agent, entry-by-entry, MS spot-confirmed on V3-14/15/16]** Alignment agent read each cited line rather than trusting the log; ran `pytest tests/ -q` → 22 passed. |
| **V-12** | **The circularity attack on the on-policy collector still fails,** for the reason `P0G_REVIEW.md` §B already gives: prediction targets come from the physics engine, so states where `c₀` is wrong enter the dataset with correct labels. Nothing found in this audit changes that. Also unchanged: `atlas/score.py::umf` and `atlas/stats.py` remain unmodified (`git status` confirms), and `run_e0_planning.py`'s planning loop was correctly left untouched. | **[agent + MS]** `git status` read at session start; no diff to either protected file. |

### COSMETIC

| ID | Finding | Evidence |
|---|---|---|
| **C-1** | Non-strict JSON in two artifacts: `frozen_check/results.json` has bare `NaN` (`"train_loss": NaN`) and `frozen_check/val_loss_ln_act_R2.json` has bare `Infinity`. Python round-trips both; `jq`, JS and Rust parsers reject them. From `harness.py:153,252-257` serialising `float("inf")` unchanged. **[MS-confirmed — read both files verbatim.]** |
| **C-2** | `final_loss` now holds the best-val-step loss — the variable name asserts the property V3-15 specifically removed (`run_e0.py:1257,1292,1378,1380`). |
| **C-3** | `dump_regime_chunks` is imported but never used in `merge_p0g_shards.py:33` — merged chunks are concatenated from shard files, never recomputed; the import implies a recomputation that does not happen. |
| **C-4** | `analyze_cost_ranking.py`'s documented invocation is broken: the docstring (`:17-18`) passes `--regime`, but `main()` (`:68-70`) registers only `--json`. Worth fixing before C-1 is run from the docstring. |
| **C-5** | The sharding seed formula depends on `--collect-max-tries`, which is in neither `_traj_guard` nor `_p0g_flags` (`run_e0.py:221,1095`). Shards launched with differing values would produce colliding seeds the guard cannot see. **Contained, not silent** — the merge's overlap check (`:78-83`) would reject. The real run's arithmetic checks out (100 trajs × `max_tries=2` → seeds 1000–1198). |
| **C-6** | FIXLOG V3-15's claim that `block_static_R2.json`'s 6/1/0 and the manifest's zero-contact counts are "two independently-computed diagnostics [that] agree" is over-stated. The R2 numbers do coincide (6/1/0), but **zero-contact and block-static are different predicates**, and for R0 they do *not* coincide: block-static **8/1/2** vs zero-contact **6/1/2**. The R2 agreement is a coincidence, not cross-validation. **[MS-confirmed: recomputed both counts per split for both regimes.]** |

---

## THE FOUR GENERAL PROPERTIES, ANSWERED

**(a) Does every FIXLOG "DONE" correspond to a change in current source?** **Yes — see V-11.** Every
fix V3-9…V3-16 names physically exists at the cited responsibility; only line numbers drift. **But
the property fails one level out:** V3-15's Bug-4 fix reached the code and R2's artifact and never
R0's (T-1), and V3-15's `train_loss` patch reached two of three artifacts (T-2). *A fix present in
code is not the same as a fix propagated to the artifacts that fix invalidated* — that is the
generalised form, and it is the single most productive lens this audit had.

**(b) Does every required human decision have a dated, written record?** **One of four.** R1's drop
is properly dated and written into §8.1/§8.3 (V-1) — a genuine improvement over this project's
history. `iterations = 10` is recorded only as a plan-table cell with no sign-off line. τ and the
motion gate are correctly held open under §15-5. **The §15-2 damping decision had its money spent
before the check was run (B-2).** Nothing was goalpost-moved; the *record* §1.8 requires does not
exist for three of the four.

**(c) Does every quantitative claim have a rerun-and-diff, or an UNVERIFIED label?** **No.** The
single R2 fine-tune has never been rerun and diffed; §4.1's P16 determinism-asymmetry check is still
`PREPARED, NOT RUN`, and the `--steps 0` control has no backward pass so it does not exercise it.
Every number in `results.json` is a single-launch value carrying neither a repeat nor a label
(B-5). §4.1's own recipe costs ~6 min and ~$0.15.

**(d) Does every comparison compare like-for-like?** **The fourth instance is B-4, and it is about
to be committed to.** Trajectory-scale-vs-chunk-scale (§7-B2), wrong-gate-in-the-report (V3-15 Bug 4),
and smoke-population-vs-test-population (V3-15/16) all had one shape: *two numbers on different
scales or populations compared as though they were the same quantity.* B-4 is that shape moved into
the **planner protocol**, and it is worse than its three predecessors because the mismatch would be
invisible in the output — the screen would print a clean McNemar over mismatched arms. **T-1** (R0's
uncorrected gate), **T-3** (unpaired R0/R2 task populations) and **T-10** (stride-1 vs aligned
windows) are the fifth, sixth and seventh instances, all currently live on disk. The class is not
fenced: **H-4** shows §6.6's own "assert it in G6" invariant was never implemented.

---

## MINIMUM WORK TO CLOSE P0

**Right now, before C-2's output is read** (minutes):
1. **B-4** — confirm the live C-2 launch passed `--iterations 10 --num-act-stepped 2`. If it did not,
   discard and relaunch; the pairing is invalid.
2. **T-14** — confirm the live C-1/C-2 runs point at `p0g_onpolicy/`, **not** `p0g_onpolicy_frozen_check/`,
   which holds an identically-named identity chart. A run against the wrong directory compares frozen
   against frozen and returns a clean null that looks exactly like a real result.
3. **H-1** — rule on RED FLAG 2's semantics under a null, in writing, *before* seeing the number.

**Free / model-free** (do all before any further GPU spend):
4. **T-1** — re-derive `gate_calibration_R0.json` at the T=2 gate; correct FIXLOG V3-16's table
   (R2's 10-pct false-pass is **0.25**, not 0.0%). Blocked on B-6 for the exact value; the
   64–66% range is already established.
5. **B-2** — apply §15-2's pre-registered rule to R0 17.07 vs R2 4.50 and **write the dated decision
   into §4/§15-2**, whichever way it goes.
6. **B-1** — adopt τ and q as a matched pair from the on-policy derivation, and record it.
7. **B-3** — add P0-G rows to `EVIDENCE_LEDGER.md` §5 and `IMPLEMENTATION_PLAN_V3.md` §11.1-R.
8. **T-2** — regenerate `results.md`.
9. **B-6** — re-download `trajs_R0.pt` and verify with `torch.load` + a shape assert.
10. **H-2** — enforce the `--out-subdir` collision rejection before the next sharded launch.

**Cheap GPU** (~$0.35 total):
11. **B-5** — dump per-trajectory and per-window UMF for both arms; report Δ with a paired-bootstrap CI.
12. **§4.1 / P16** — the determinism-asymmetry check, ~6 min.
13. **H-5** — the forward pass that re-derives σ_r and the strike rate on-policy.

---

## STANDING CAVEATS ON THIS DOCUMENT

- Every number stated as verified was read from or recomputed against a real file in this repository.
  Where a claim originated with a sub-agent and was re-derived here, the Verification column says so.
  Where it could not be re-derived — **T-3**'s 2/100 and +0.370, **T-7**'s 0.741/0.501, **T-10**'s
  aligned-vs-straddling P95s, **T-9**'s green-test demonstration, **T-12**'s `TypeError`, **H-6**'s
  50/100 — it is labelled **UNVERIFIED** and must not be quoted as established.
- **B-4**'s protocol mismatch is a *risk*, not an observed defect: the C-2 run was in flight during
  this audit and its launch command was not inspected. What is verified is that the defaults are
  wrong for it and that nothing downstream would detect it.
- **V-3** was verified against the downloaded artifact of a real Modal run, **not re-executed** — a
  re-run is Modal spend and this was a review-only pass.
- No sample-constancy claim is made without stating the sample; see **B-1** for the on-policy vs
  dataset-replay disclosure, per `CLAUDE.md` §5.
- One prior audit item is retired here: **`P0G_REVIEW.md` P13b's R2 concern and the red-team's Q1
  are resolved by V-2's executed run** — R2's collection and the planning evaluator share zero demo
  episodes at the current post-§3.2 protocol.

---

## **P0 IS NOT CLOSED**

— because Phase 0's own stated exit condition (the §11.1 re-derivation of τ / gate / strike rate /
σ_r against on-policy chunks) is unmet and, where this audit performed it, **changes `q`**; because
§15-2's pre-registered contact rule has now been measured at −74% and never applied, so `damping =
0.5` is authorised by an unread check; because the two documents `CLAUDE.md` designates as the
project's status and numbers records both still say P0-G has not run; because the close-out's
headline result is two bare means whose CI cannot be computed from anything that was persisted;
because the R0 raw artifact does not load; and because the C-2 screen now in flight cannot detect
the protocol mismatch its own launcher defaults invite.

**None of that impugns the chart itself.** The R2 `ln_act` chart is real, its improvement over
frozen `c₀` is a genuine paired measurement on a disjoint test split, and it has no leakage. What is
missing is the record, the re-derivation, and the two acceptance checks — and every one of those is
cheap.
